apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: dp-system
spec:
  ingressClassName: traefik
  rules:
    - host: grafana.ml.ink
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 80
